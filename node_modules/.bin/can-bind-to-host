#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../can-bind-to-host/dist/bin/can-bind-to-host.js" "$@"
  ret=$?
else 
  node  "$basedir/../can-bind-to-host/dist/bin/can-bind-to-host.js" "$@"
  ret=$?
fi
exit $ret
